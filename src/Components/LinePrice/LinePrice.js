import React from 'react'
import style from './LinePrice.module.scss'

const LinePrice = () => {
  return (
    <div className={`${style.OuterContainer}`}>
        <span className={`${style.linePriceSpan}`}>11/153/00</span>
        
    </div>
  )
}

export default LinePrice